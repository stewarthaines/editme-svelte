# Sample Magazine — Outline

This sample illustrates responsive layout and image handling in SEED.html — a designed, art-directed magazine that still reflows. Source is Djot (classed spans/divs, comments); the project installs a custom heading font and a responsive grid (`Styles/page.css` + `Scripts/responsive.js`, one column to two at the breakpoints).

Production notes:

- No hard-wrapping — one line per paragraph (list items stay one line each).
- Tone: demonstrate, don't declaim — the issue is the argument; caveats stay honest ("on supported Reading Systems").

## Your Sample Starts Here (drafted)

- Bilingual welcome (lang-fr span), custom-classed title treatment

## Key Features (drafted)

- Feature list inside the responsive `cols` div: custom heading font, responsive layout, embedded audio, captioned figures
- Captioned image figure (webp)

## Make one of your own (drafted)

- The pitch, measured: built with html/css/javascript (designers/developers already speak it); issue content is plain text + images; the book carries its own source — verify in SEED.html

## Coming later

- More spreads as the draft grows (photo essay, pull-quotes are candidates, not commitments)
