{
  "name": "seed-plugin-agent-bridge",
  "version": "0.1.0",
  "private": true,
  "description": "Dev-only spike: volunteers read-only project access to a local agent bridge (scripts/agent-bridge.mjs). Never ships — devOnly keeps it out of the production plugin manifest.",
  "seedhtmlPlugin": {
    "id": "agent-bridge",
    "name": "Agent Bridge (dev)",
    "presentation": "panel",
    "buildEntry": "plugin.html",
    "devOnly": true
  }
}
